# Implementation Target Vocabulary

## Purpose

Implementation target describes the intended component implementation
environment for a later plan. It does not mean the `ComponentSpec` step should
write code.

## Framework Values

- `react`

## Required Format Field

`defaultComponentCodeFormat` must use the component code format vocabulary.
